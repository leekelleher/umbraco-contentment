@inherits UmbracoViewPage<ContentPage>
@{
    Layout = "_layout.cshtml";
}

@section body
{
    @if (Model.PageBlocks != null)
    {
        foreach (var block in Model.PageBlocks)
        {
            @await Html.PartialAsync($"blocks/{block.ContentType.Alias}", block)
        }
    }
}
